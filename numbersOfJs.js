let score = 400
// console.log(score);

let balance = new Number(100)
// console.log(balance);

let newNumber = 123.255154
// console.log(typeof newNumber.toFixed(2)); //toFixed() will change the datatype from number to strings.

let numTwo = 100000000
// console.log(numTwo.toLocaleString());


// Math.random will be the most used case in Js math.

// go teg number in a range

let min = 10
let max = 20

console.log(Math.floor(Math.random() * (max - min + 1)) + min);